# Every text file checks out with LF, on every platform. The wagglebot shell block
# sources .env.credentials with `.`, and bash rejects a CRLF copy of a file with
# `$'\r': command not found`. A Windows clone with core.autocrlf=true would produce
# exactly that, and WSL would then fail to load the credentials.
* text=auto eol=lf
