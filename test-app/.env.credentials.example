# For a working tree, copy this file to .env.credentials (gitignored).
# For cached mode, use ~/.wagglebot/.env.credentials outside cache revisions.
# Add one line for each
# credential that a registry entry names. The values stay on this machine.
# EXAMPLE_TOKEN=
